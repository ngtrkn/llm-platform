# Example Training Strategy Configuration
# This file can be used to customize training parameters
# Upload via API or place in strategies/ directory

# Learning rate schedule
lr0: 0.01          # Initial learning rate
lrf: 0.01         # Final learning rate (lr0 * lrf)
momentum: 0.937   # SGD momentum/Adam beta1
weight_decay: 0.0005  # Weight decay

# Augmentation
hsv_h: 0.015      # Image HSV-Hue augmentation
hsv_s: 0.7        # Image HSV-Saturation augmentation
hsv_v: 0.4        # Image HSV-Value augmentation
degrees: 0.0      # Image rotation (+/- deg)
translate: 0.1    # Image translation (+/- fraction)
scale: 0.5        # Image scale (+/- gain)
shear: 0.0        # Image shear (+/- deg)
perspective: 0.0  # Image perspective (+/- fraction)
flipud: 0.0       # Image flip up-down (probability)
fliplr: 0.5       # Image flip left-right (probability)
mosaic: 1.0       # Image mosaic (probability)
mixup: 0.0        # Image mixup (probability)

# Loss weights
box: 7.5          # Box loss gain
cls: 0.5          # Class loss gain
dfl: 1.5          # DFL loss gain

# Other parameters
warmup_epochs: 3.0  # Warmup epochs
warmup_momentum: 0.8
warmup_bias_lr: 0.1
close_mosaic: 10   # Disable mosaic augmentation for final N epochs
